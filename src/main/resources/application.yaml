server:
  port: 1200
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306"
    username: "root"
    password: "root"
  sql:
    init:
      mode: ALWAYS
  jpa:
    properties:
      hibernate.format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true